#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <windows.h>`n`nextern "C" void __cdecl ___crtCorExitProcess(int param_1)`n{`n    HMODULE hModule = GetModuleHandleW(L"mscoree.dll");`n`n    if (hModule != nullptr)`n    {`n        FARPROC pFVar1 = GetProcAddress(hModule, "CorExitProcess");`n`n        if (pFVar1 != nullptr)`n        {`n            reinterpret_cast<void (__stdcall*)(int)>(pFVar1)(param_1);`n        }`n    }`n}`n