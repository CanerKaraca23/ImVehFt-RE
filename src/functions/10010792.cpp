#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstdint>`n`nstruct TypeInfoStorage`n{`n    bool __thiscall equals(const TypeInfoStorage* other) const;`n    void** vtable; // offset 0; type name begins at offset 9`n};`n`nbool TypeInfoStorage::equals(const TypeInfoStorage* other) const`n{`n    using StrCmpFn = int (__cdecl*)(const char*, const char*);`n    const auto left = reinterpret_cast<const char*>(other) + 9;`n    const auto right = reinterpret_cast<const char*>(this) + 9;`n    return reinterpret_cast<StrCmpFn>(static_cast<std::uintptr_t>(0x10013b50))(left, right) == 0;`n}`n