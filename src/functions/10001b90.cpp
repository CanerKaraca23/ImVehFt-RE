#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <windows.h>`n#include <stdio.h>`n#include <windows.h>`n#include <cstdint>`n#include <cstring>`n`nextern "C" void* __cdecl _memset(void*, int, std::size_t);`n`nextern std::uint16_t DAT_100243dc;`n`nextern int __cdecl _strncmp(`n    char* str1,`n    char* str2,`n    std::uint32_t max_count);`n`nusing Callback = void (__cdecl*)(char*);`n`nextern std::int32_t __cdecl FUN_10001b90(`n    LPCSTR param_1,`n    Callback param_2);`n`nstd::int32_t __cdecl FUN_10001b90(`n    LPCSTR param_1,`n    Callback param_2)`n{`n    char local_460[320];`n    CHAR local_320[264];`n    char local_218[264];`n    CHAR local_110[268];`n    char* local_464;`n`n    _memset(local_460, 0, 0x140);`n`n    DWORD result = GetFullPathNameA(`n        param_1,`n        0x104,`n        local_110,`n        &local_464);`n`n    if (result == 0)`n        return 0;`n`n    char* source = local_464;`n    char* destination = local_218;`n`n    do`n    {`n        char value = *source;`n        *destination = value;`n        ++source;`n        ++destination;`n    } while (destination[-1] != '\0');`n`n    std::memcpy(local_464, "*.*", 4);`n`n    *reinterpret_cast<std::uint32_t*>(local_460) = 0x10;`n`n    HANDLE find_handle = FindFirstFileA(`n        local_110,`n        reinterpret_cast<LPWIN32_FIND_DATAA>(local_460));`n`n    if (find_handle != INVALID_HANDLE_VALUE)`n    {`n        do`n        {`n            if (_strncmp(local_460 + 0x2c, const_cast<char*>("."), 1) != 0 &&`n                _strncmp(local_460 + 0x2c, const_cast<char*>(".."), 2) != 0 &&`n                ((*reinterpret_cast<std::uint32_t*>(local_460) & 0x10) != 0))`n            {`n                result = GetFullPathNameA(`n                    param_1,`n                    0x104,`n                    local_320,`n                    &local_464);`n`n                if (result == 0)`n                    return 0;`n`n                source = local_460 + 0x2c;`n                destination = local_464;`n`n                do`n                {`n                    char value = *source;`n                    *destination = value;`n                    ++source;`n                    ++destination;`n                } while (destination[-1] != '\0');`n`n                auto* word_pointer =`n                    reinterpret_cast<std::uint16_t*>(local_460 + 0x13f);`n`n                char* next;`n                do`n                {`n                    next = reinterpret_cast<char*>(word_pointer) + 1;`n                    word_pointer =`n                        reinterpret_cast<std::uint16_t*>(reinterpret_cast<char*>(word_pointer) + 1);`n                } while (*next != '\0');`n`n                *word_pointer = DAT_100243dc;`n`n                source = local_218;`n                do`n                {`n                    ++source;`n                } while (source[-1] != '\0');`n`n                std::uint32_t length =`n                    static_cast<std::uint32_t>(source - local_218);`n`n                destination = local_460 + 0x13f;`n`n                do`n                {`n                    next = destination + 1;`n                    destination = destination + 1;`n                } while (*next != '\0');`n`n                source = local_218;`n`n                for (std::uint32_t count = length >> 2;`n                     count != 0;`n                     --count)`n                {`n                    *reinterpret_cast<std::uint32_t*>(destination) =`n                        *reinterpret_cast<const std::uint32_t*>(source);`n`n                    source += 4;`n                    destination += 4;`n                }`n`n                for (std::uint32_t count = length & 3;`n                     count != 0;`n                     --count)`n                {`n                    *destination = *source;`n                    ++source;`n                    ++destination;`n                }`n`n                FUN_10001b90(local_320, param_2);`n            }`n`n        } while (FindNextFileA(`n            find_handle,`n            reinterpret_cast<LPWIN32_FIND_DATAA>(local_460)) != 0);`n    }`n`n    FindClose(find_handle);`n`n    find_handle = FindFirstFileA(`n        param_1,`n        reinterpret_cast<LPWIN32_FIND_DATAA>(local_460));`n`n    if (find_handle != INVALID_HANDLE_VALUE)`n    {`n        do`n        {`n            if ((*reinterpret_cast<std::uint32_t*>(local_460) & 0x10) == 0)`n            {`n                result = GetFullPathNameA(`n                    param_1,`n                    0x104,`n                    local_218,`n                    &local_464);`n`n                if (result == 0)`n                    return 0;`n`n                source = local_460 + 0x2c;`n                destination = local_464;`n`n                do`n                {`n                    char value = *source;`n                    *destination = value;`n                    ++source;`n                    ++destination;`n                } while (destination[-1] != '\0');`n`n                param_2(local_218);`n            }`n`n        } while (FindNextFileA(`n            find_handle,`n            reinterpret_cast<LPWIN32_FIND_DATAA>(local_460)) != 0);`n`n        FindClose(find_handle);`n    }`n`n    return 1;`n}`n