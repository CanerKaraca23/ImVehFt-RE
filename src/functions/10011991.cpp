#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstdint>`n`nextern "C" int __cdecl _isdigit(unsigned int value);`n`nextern "C" unsigned int __cdecl __hextodec(std::uint8_t param_1)`n{`n    const int is_digit = _isdigit(static_cast<unsigned int>(param_1));`n    unsigned int value =`n        static_cast<unsigned int>(static_cast<char>(param_1));`n`n    if (is_digit == 0)`n    {`n        value = (value & 0xffffffdfu) - 7u;`n    }`n`n    return value;`n}`n