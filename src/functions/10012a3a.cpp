#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstddef>`n#include <cstdint>`n`nextern "C" void* __cdecl __calloc_impl(`n    std::size_t count,`n    std::size_t size,`n    std::uint32_t* parameter);`n`nextern "C" void __stdcall Sleep(std::uint32_t milliseconds);`n`nextern "C" std::uint32_t DAT_10039a08;`n`nextern "C" void* __cdecl __calloc_crt(`n    std::size_t count,`n    std::size_t size)`n{`n    std::uint32_t milliseconds = 0;`n`n    while (true)`n    {`n        void* result = __calloc_impl(count, size, nullptr);`n`n        if (result != nullptr)`n        {`n            return result;`n        }`n`n        if (DAT_10039a08 == 0)`n        {`n            break;`n        }`n`n        Sleep(milliseconds);`n        milliseconds = milliseconds + 1000;`n`n        if (DAT_10039a08 < milliseconds)`n        {`n            milliseconds = 0xffffffffu;`n        }`n`n        if (milliseconds == 0xffffffffu)`n        {`n            return nullptr;`n        }`n    }`n`n    return nullptr;`n}`n