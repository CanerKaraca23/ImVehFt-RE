#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstddef>`n#include <cstdint>`n`nextern std::uint32_t DAT_10039a4c;`n`nextern "C" void* __stdcall DecodePointer(void* Ptr);`n`nextern "C" int __cdecl __callnewh(std::size_t _Size)`n{`n    using NewHandler = int(__cdecl*)(std::size_t);`n`n    auto handler = reinterpret_cast<NewHandler>(`n        DecodePointer(reinterpret_cast<void*>(`n            static_cast<std::uintptr_t>(DAT_10039a4c))));`n`n    if (handler != nullptr) {`n        int result = handler(_Size);`n        if (result != 0) {`n            return 1;`n        }`n    }`n`n    return 0;`n}`n