#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstdint>`n`nextern "C" void __cdecl __lock(int lock_number);`nextern "C" void __stdcall EnterCriticalSection(void* critical_section);`n`nextern "C" void __cdecl __lock_file2(int _Index, void* _File)`n{`n    if (_Index < 0x14)`n    {`n        __lock(_Index + 0x10);`n`n        auto* flags = reinterpret_cast<std::uint32_t*>(`n            reinterpret_cast<std::uint8_t*>(_File) + 0x0c);`n`n        *flags = *flags | 0x8000;`n        return;`n    }`n`n    EnterCriticalSection(`n        reinterpret_cast<std::uint8_t*>(_File) + 0x20);`n}`n