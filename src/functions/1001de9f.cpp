#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`nextern "C" int* __cdecl __errno(void);`n#include <cstdint>`n`nusing errno_t = int;`n`nextern "C" std::uint32_t __cdecl __control87(`n    std::uint32_t newValue,`n    std::uint32_t mask);`n`nextern "C" int* __cdecl __errno();`n`nextern "C" void __stdcall FUN_1001189f();`n`nextern "C" errno_t __cdecl __controlfp_s(`n    std::uint32_t* currentState,`n    std::uint32_t newValue,`n    std::uint32_t mask)`n{`n    std::uint32_t maskedValue = mask & 0xfff7ffffU;`n    errno_t result;`n`n    if ((newValue & maskedValue & 0xfcf0fce0U) == 0U)`n    {`n        if (currentState == nullptr)`n        {`n            __control87(newValue, maskedValue);`n        }`n        else`n        {`n            maskedValue = __control87(newValue, maskedValue);`n            *currentState = maskedValue;`n        }`n`n        result = 0;`n    }`n    else`n    {`n        if (currentState != nullptr)`n        {`n            maskedValue = __control87(0U, 0U);`n            *currentState = maskedValue;`n        }`n`n        int* errorNumber = __errno();`n        result = 0x16;`n        *errorNumber = 0x16;`n        FUN_1001189f();`n    }`n`n    return result;`n}`n