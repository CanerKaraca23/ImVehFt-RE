#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <intrin.h>`n#include <stdio.h>`n#include <cstdint>`n#include <intrin.h>`n`nextern "C" void __cdecl FUN_10017cd2(std::int32_t param_1);`n`nextern "C" void __stdcall FUN_1001803c(void)`n{`n    // Uses the caller's EBP-relative value at offset -0x1C.`n    // The exact source-level object corresponding to this value is unresolved.`n    if (*reinterpret_cast<std::int32_t*>(`n            reinterpret_cast<std::uintptr_t>(_AddressOfReturnAddress()) - 0x20) != 0)`n    {`n        FUN_10017cd2(0);`n    }`n}`n