#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstddef>`n#include <cstdint>`n`nstruct ExceptionStorage`n{`n    void __thiscall tidy();`n    void* vtable;                  // +0x00`n    char* what;                    // +0x04`n    std::uint8_t do_free;          // +0x08`n};`n`nstatic_assert(offsetof(ExceptionStorage, what) == 4);`nstatic_assert(offsetof(ExceptionStorage, do_free) == 8);`n`nvoid ExceptionStorage::tidy()`n{`n    if (this->do_free != 0)`n    {`n        using FreeFn = void (__cdecl*)(void*);`n        reinterpret_cast<FreeFn>(static_cast<std::uintptr_t>(0x100116db))(this->what);`n    }`n`n    this->what = nullptr;`n    this->do_free = 0;`n}`n