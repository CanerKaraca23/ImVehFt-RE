#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstddef>`n#include <cstdint>`n`nextern "C" void __cdecl FUN_10017cd2(std::int32_t param_1);`n`nextern "C" __declspec(naked) void __stdcall FUN_10012dc7(void)`n{`n    // Ghidra shows this as a frameless helper that reads its caller's EBP+0x10.`n    // Keep the incoming EBP and caller-cleaned stack ABI rather than substituting`n    // this function's own frame or return-address-relative storage.`n    __asm {`n        cmp dword ptr [ebp + 10h], 0`n        jz no_call`n        push 8`n        call FUN_10017cd2`n        pop ecx`n    no_call:`n        ret`n    }`n}`n