#include <cstddef>`n#include <cstdint>`n#include <corecrt.h>`n#include <stdio.h>`n#include <cstdint>`n`n// Applies the caller-selected x87 rounding-control bits and masks exceptions.`n// The original routine is a leaf thunk that deliberately reads its argument`n// directly from the x86 stack and leaves stack cleanup to the caller.`nextern "C" __declspec(naked) std::uint32_t __cdecl FUN_1001c9a5(void)`n{`n    __asm {`n        mov edx, dword ptr [esp + 4]`n        and edx, 0300h`n        or edx, 07fh`n        mov word ptr [esp + 6], dx`n        fldcw word ptr [esp + 6]`n        ret`n    }`n}`n